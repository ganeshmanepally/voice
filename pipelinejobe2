pipeline{
    agent any
    stages{
        stage('git checkout'){
            steps{
                git url: 'https://github.com/ganeshmanepally/voice.git', branch: 'master'
            }
        }
        stage(build){
            steps {
                sh 'ls'
            }
        }
        stage("checking path"){
            steps {
                sh 'pwd'
            }
        }
    }
}